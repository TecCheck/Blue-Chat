<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BlueChat" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/blue_light</item>
        <item name="colorPrimaryVariant">@color/blue_light</item>
        <item name="colorOnPrimary">@color/foreground_dark</item>
        <!-- Secondary colors -->
        <item name="colorSecondary">@color/magenta_light</item>
        <item name="colorSecondaryVariant">@color/magenta_light</item>
        <item name="colorOnSecondary">@color/foreground_dark</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorOnBackground">@color/foreground_light</item>
        <!-- Surface colors -->
        <item name="colorSurface">@color/surface_light</item>
        <item name="colorOnSurface">@color/foreground_light</item>
        <!-- Error colors -->
        <item name="colorError">@color/red_light</item>
        <item name="colorOnError">@color/foreground_light</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>
</resources>